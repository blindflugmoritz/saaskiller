# Frontend .env — copy to .env and fill in as needed.
# PUBLIC_* vars are exposed to the browser.

PUBLIC_API_URL=http://localhost:8000/api

# === FEATURE: sentry ===
PUBLIC_SENTRY_DSN=
PUBLIC_ENVIRONMENT=development
# === END FEATURE: sentry ===
